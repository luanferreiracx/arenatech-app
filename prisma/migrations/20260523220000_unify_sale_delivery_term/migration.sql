-- Decisao revertida: termo de entrega e termo de responsabilidade
-- ficam UNIFICADOS em um unico documento/assinatura (signature*).
-- Quando a venda tem upgrade, o PDF do termo de entrega ja inclui o
-- bloco de responsabilidade pelos aparelhos recebidos.
-- Esta migration nao adiciona colunas — manteve-se a estrutura original.
SELECT 1;
